# BSC-KPI reference implementation evidence

## T011 — PostgreSQL organization authorization persistence

- Date: 2026-08-13
- Database target: `kpi_lab_test`
- Migration command: `./harness.cmd migrate`
- Migration result: the nine existing migrations were already applied with matching checksums and `202608130001_AuthorizationFoundation` applied successfully.
- Focused command: `dotnet test tests/Kpi.IntegrationTests/Kpi.IntegrationTests.csproj --no-restore --configuration Release --filter "FullyQualifiedName~OrganizationAuthorizationPostgresTests|FullyQualifiedName~KpiMigrationRunnerTests" -m:1`
- Result: 8 passed, 0 failed, 0 skipped.
- Covered behavior: migration ledger/checksum/idempotency, local target allow-list, composite Organization FK isolation, append-only approved baseline/segment facts, and PostgreSQL `xmin` optimistic concurrency.
- Exact requirement traceability: `FR-001`/`FR-002` cover composite Organization FK isolation; `FR-013`/`FR-037` cover append-only baseline and applicability facts; `FR-036` covers stale `xmin` concurrency; `FR-033` covers authorization evidence columns and audit append behavior.
- Composition boundary: `OrganizationAuthorizationSchemaTests` plus `PostgresCompositionTests`/`PostgresRuntimeSelectionTests` verify that runtime persistence uses only `ConnectionStrings:KpiRuntime` and migration-only configuration does not register the runtime `KpiDbContext`.
- Post-refactor verification (2026-08-13, supplied terminal output): `./harness.cmd migrate` targeted `kpi_lab_test` with all declared migrations already applied, then the focused suite completed with 8 passed, 0 failed, and 0 skipped. This satisfies the T016 mapping/refactor verification gate.
- 2026-08-13 local verification: solution Release build passed with 0 warnings and 0 errors; integration tests passed 48 with 8 PostgreSQL tests skipped because opt-in variables were not set in this session.
- 2026-08-13 opt-in verification: `KPI_POSTGRES_TESTS=1`, `Kpi__TestDatabaseName=kpi_lab_test`, and the migration connection targeted the local allow-listed PostgreSQL database; the complete `Kpi.IntegrationTests` assembly passed 61/61 with 0 failed and 0 skipped.
- 2026-08-13 canonical bootstrap: `./harness.cmd bootstrap` passed locked restore, Release Playwright-driver build, and pinned Chromium provisioning after the IntegrationTests lock file was regenerated for the migrator project reference.
- 2026-08-13 rerun `./harness.cmd check`: repository contract, locked bootstrap, lint, all .NET tests, integration-guide, branch-policy, migration-command, and runtime-profile checks passed; the default no-credential profile intentionally reported 8 PostgreSQL skips.
- T024 foundational focused verification: Domain authorization value-object/audit tests, Application freshness/catalog/unit-of-work tests, migration/Problem Details/composition tests, and the opt-in PostgreSQL migration suite all passed; no User Story implementation is included in this checkpoint.
- 2026-08-13 Phase-2 remediation: `FR-033` application test proves KPI Definition persistence and its Audit Record execute inside one shared transaction callback; the PostgreSQL rollback test also proves a command failure after `SaveChanges` leaves both business and audit rows absent.
- 2026-08-13 `FR-023`/`FR-024`/`FR-031`/`FR-049` authorization-facts verification: the PostgreSQL reader reloads active role versions, fixed-catalog capabilities, effective assignments/scopes, approved baseline segments, represented-employee status, and active delegation facts; the test confirms role retirement and assignment revocation are observed by a new action while the same action context remains stable.
- 2026-08-13 `FR-027` Problem Details verification: stable `MALFORMED_REQUEST`/`AUTHORIZATION_DENIED`/`RESOURCE_NOT_FOUND`/`CONCURRENCY_CONFLICT`/`VALIDATION` codes map deterministically to 400/403/404/409/422 even when the caller supplies status 500; correlation, concurrency-token, baseline, and segment extensions remain present.
- 2026-08-13 Phase-1/Phase-2 gate reconciliation: T001-T008 and T018-T020/T024 are checked only after their exact test/evidence artifacts exist; the gate text explicitly keeps User Story work and both target repositories locked pending human approval.
- 2026-08-13 canonical verification: `./harness.cmd bootstrap` passed; elevated `./harness.cmd check` passed repository contract, locked restore, lint, 32 Application tests, 32 Domain tests, 62 Integration tests with the default PostgreSQL skips, 4 Playwright tests, integration-guide, branch-policy, migration-command, and runtime-profile checks.
- 2026-08-13 pre-remediation opt-in PostgreSQL verification: `./harness.cmd migrate` targeted allow-listed `kpi_lab_test` with all 11 declared migrations already applied; the complete `Kpi.IntegrationTests` assembly passed 72/72 with 0 failed and 0 skipped. The final remediation run is recorded below at 74/74.
- 2026-08-13 pre-remediation traceability rerun: foundational test display names retain exact `FR-xxx` identifiers across decision, catalog, persistence, migration, composition, Problem Details, transaction, and current-facts coverage.
- Credentials: not recorded.
- 2026-08-13 authorization remediation: `PostgresAuthorizationFactsReader` now intersects the requested capability with the exact active Role Version before selecting assignments, scope evidence, and delegation scope; a two-role/two-scope PostgreSQL test proves no capability/scope cross-product is possible (`FR-023`/`FR-024`/`FR-049`).
- 2026-08-13 authorization remediation: every production `KpiOperations` command is composed with `IAuthorizationDecision`; the FR-049 Application test changes the decision between two commands and observes the second denial. The Postgres runtime composition test resolves `KpiOperations` with the shared seam.
- 2026-08-13 freshness remediation: one EF context plus separate PostgreSQL connections proves account disablement, employment end, and resource revision changes are observed by the next action; focused `PostgresAuthorizationFactsTests` result was 3 passed, 0 failed, 0 skipped (`FR-006`/`FR-036`/`FR-049`).
- 2026-08-13 canonical remediation verification: `./harness.cmd bootstrap` passed with 0 warnings/errors; `./harness.cmd check` passed repository contract, lint, Application 33/33, Domain 32/32, Integration 62 passed/12 default PostgreSQL skips, Playwright 4/4, guide, branch, migration, and runtime-profile checks.
- 2026-08-13 opt-in remediation verification: `./harness.cmd migrate` targeted allow-listed `kpi_lab_test` with all 11 declared migrations already applied; the complete `Kpi.IntegrationTests` assembly passed 74/74, 0 failed, 0 skipped.
- 2026-08-13 Phase-2 gate reachability check: `6ab91e1` and `1d83d19` are not ancestors of the reference HEAD (exit code 1 for both), `git log --all` contains no User Story organization-baseline files after removing the local backup tag, and the current `src/Kpi.Domain/Organizations` tree contains only `EffectiveInterval.cs`, `Organization.cs`, and `SharedValueObjects.cs`. Both target repositories remain untouched.
