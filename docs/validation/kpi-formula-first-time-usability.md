# First-time formula-user validation protocol

Use a participant who has not seen the editor. Ask them to add two variables,
enter `revenue / target * 100`, validate it and run a Test Run. Record only
anonymized participant id, completion, observed issue, date, environment and
whether assistance was needed. Calculate completion percentage and compare it
with SC-010's 90% threshold. This file intentionally contains no invented
participant result.
