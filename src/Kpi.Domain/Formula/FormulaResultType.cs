namespace Kpi.Domain.Formula;

/// <summary>Declared result types supported by the MVP.</summary>
public enum FormulaResultType
{
    Decimal,
    Boolean
}

/// <summary>Formula variable data types.</summary>
public enum FormulaValueType
{
    Decimal,
    Boolean
}
