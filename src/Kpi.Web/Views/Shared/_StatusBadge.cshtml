@model string?
@{
    var value = string.IsNullOrWhiteSpace(Model) ? "Unknown" : Model;
    var css = value.ToLowerInvariant().Replace(" ", "-");
}
<span class="status-badge status-@css">@value</span>
